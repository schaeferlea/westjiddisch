pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - find . -mindepth 1 -maxdepth 1 -not -name public -not -name '.*' -exec cp -r {} public/ \;
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
